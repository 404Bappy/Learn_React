import React from 'react';

const Config = (props) => {
    return (
        <div className='ConfigD'>
            <ul>
                <li>price : {props.price}</li>
            </ul>
        </div>
    );
};

export default Config; 